---
title: "AI全栈开发之零基础Vue3教程教学"
description: "视频详细讲解了Vue3的基础语法、环境搭建及实战应用，适合零基础学习前端框架开发。"
pubDate: "2024-05-28"
favDate: "2024-06-09"
---
> **UP主**: 三分钟实验室 · **时长**: 26:18 · [🔗 B站原视频](https://www.bilibili.com/video/BV1W1421173W)
>
> **发布**: 2024-05-28 · **收录**: 2024-06-09
>
> **标签**: Vue3 · 前端开发 · 编程教程 · JavaScript

这是一个盒子点它会变大变颜色再点会变回来实现这个功能只需要两步声明一个布尔变量然后按照布尔值是 True或 False 动态绑定内明最后监听这个盒子的点击事件点击时把布尔值取返即可这是一个输入框输入字符下方实时显示输入的值这功能也需要两步首先冲点一个变量Vega Model绑定到输入框中然后用两个大括号把变量放在下面的盒子中即可我有一个数组希望根据数组长度动态创建盒子可以这样写在DIV后面写上v-4 item in list然后用两个大括号把变量放在盒子中就会出现这样的效果基于刚刚的逻辑我们拿出上期视频写的静态页面创建一个字符串变量用来保存输入框的值创建一个数组代表下面的代班式项列表每一项有两个字It's complete表示是否完成text代表中间的文字写一个添加函数这个函数的作用是往数组中添加一个新的值添加完毕后轻购变量再写一个删除函数这个函数接收一个参数是数组的下标功能是根据下标移数数组中的某一项然后给输入框绑定它变量今天按钮的点击事件绑定刚刚写的添加函数通过数组渲染下面的列表再给单项框绑定是否完成字段根据是否完成动态绑定样式变表中展示每项的文字最后监听一下删除按钮的点击事件绑定删除函数测试一下添加完成删除好的我们用Viu做了一个会动的代办事项页面嗨大家好欢迎来到三分钟实验室今天我们一起来学习Viu正如Viu官网介绍所说它是一个JavaScript框架易学易用它的作用呢是让GS和HTML连接起来让网页能动起来就像我们前面展示的那样在开始之前呢我们需要安装一下环境也就是Node那首先呢我们需要安装一个NVM这个东西的安装包我放在了QO群里哈大家有需要的话自取就一直下一步安装就行了安装好了之后呢打开这个命令行我们输入NVM install20意思就是安装Node的20版本然后我们输入nvmls看一下这个版本号是多少然后呢我这个是20.13.1我就输入nvmuse20.13.1意思就是使用这个版本然后呢你再输入node-v这个npm-v如果你成功的输出了这个版本号就说明是安装成功了哈node安装完之后呢我给大家准备了一个某版代码也就是我们之前教程中写过的这个静态页面这个代码呢放在QO群了大家按需自取那写完之后的完整代码呢也会放在群里面方便大家对照改错我们拿到代码之后呢把它拖入VS Code打开就会得到这样一个项目那其他的文件呢我们先不管找到SRC底下有个APP.view这个页面呢也就是我们网页中看到这个页面我们本期视频的所有内容呢都基于这个页面操作所以我们可以关掉这个测边了一个view页面呢它分为三个部分Script的标签和Template的标签和SR有标签那显而易见呢这里就是我们写GS代码的地方这里呢就是我们写html的地方那这里呢就是我们写css的地方那首先呢我们就把这个项目跑起来我们按command加勾或者是control加勾打开我们的命令行这个勾呢就是我们键盘上k左边的那个键知道吧然后我们输入第一个命令npm空格i这好命令的意思呢就是安装我们这个项目所需要的依赖第二个就是npm空格rundev那你看到这样的界面呢就说明我们的项目运行成功了你按住Ctrl点击这个网址就可以打开这样的页面和我一样了然后我们继续把命令行隐藏掉不需要了修改一下页面上的文字保存代码你会发现实时的就变成更新了那我们的项目就是运行成功了那首先呢我来看一下VIO3中变量的创建与使用在VIO3中创建变量跟普通的GS或者说VIO2是不太一样的我们需要通过一个RF的东西来创建比如我们要创建一个字符串按理说我们应该直接输入这个字符传的值就可以了但是在vue3中呢我要通过一个叫if的东西来创建我们找到这个后面divue的if然后按tab补权这样的话呢会帮我们从vue中引入一个if这个一定要引入哈不引入的话是没办法使用的最后面跟上一个小括号然后在其中输入我们字符传的值这样来创建那如果想创建数字呢就在这个小括号中输入数字如果是对象就输入对象如果是数组就输入数组都是一样的输入在小括号中那使用的时候呢分两种情况一个是在squib的标签中一个是在template的标签中如果在squib的标签中使用呢比如我们现在要把它变量的值打印出来我们直接打印然后呢这里输入变量的名字这样是不行的需要访问到这个变量的值呢需要点value这样呢就会成功的访问到这个变量的值我们检查打开控制台刷新哦我们代码没有保存哈保存一下刷新就会发现呢这个值已经被打印出来了如果我们不加点value呢打印出来是这么一个对象那在写v3代码的时候如果你发现变量值不太对就要看一下是不是忘记点value了那如果我们在template中使用这个变量的话就直接写这个变量名就可以了不需要点value了我们写入str保存就发现这里的值已经变成我们变量中的值了然后我们来了解一下事件监听比如我们现在写一个函数这个函数中我们就放一行代码就是这个打印的代码我现在需求是点击这个按键的时候执行这个函数需要怎么做呢我们首先找到这个按钮盒子在这里只需要在它前面加一个at我们想监听的是点击击数只需要点click然后写上这个击数的名字保存我们点击就发现这个变量已经被打印出来了但是没有打印出来值是因为没有点value我们数一下点value保存点击这个值已经被打印出来了在vivo中呢监听事件只需要用到一个at符号就可以了我们这里监听的是点击事件这条代码的意思是点击这个div盒子的时候就执行这个函数那at click是监听点击事件那我们是不是也可以监听一下其他事件比如说鼠标移入和移出我们这里删掉at比如mouse enter也就是鼠标移入我们写上这个函数的名字保存清除一下鼠标只要移进来就会触发这个函数同样的还有mouse leaf保存小心移入的时候没有触发移出的时候才会触发哎那如果我们想监听其他事件呢就直接在at符号后面写就可以了接下来呢是双向绑定我们的需求是这个输入框中输入的值呢实时的要存到我们刚刚创建的这个字符传变量中先把它清理哈怎么实现这个功能呢首先找到这个输入框这个输入框标签在这里哈那只需要一行代码就可以了比如说我这里写v-mod在这个双页号中呢我们直接写入这个变量的值刚刚讲过了在template中使用变量呢不需要点vlog直接写变量名就可以那如何验证这个值有没有被绑定进来呢我们监听一下这个点击时间点击的时候把这个值给打印出来这里我们写入add click然后add函数出发保存好我们在这里输入123123点是123123对吧我们输入33333打出来是333这个veganmodel我们叫双向绑定什么叫双向绑定呢也就是说输入框中的值会实时的保存到这个变量种而变量中的值呢也会实时的传入到输入框中怎么来验证这个呢我们在这个函数中呢多写一号代码我们给修改一下这个变量的值比如说等于个888然后保存当我们点击的时候呢这个变量的值就会被改成4个8我们点一下试试你会发现输入框中同时也输入了4个8这个值这个语义法呢就叫双向绑定除了输入框可以使用这个双向绑定呢我们通过输入框实现的单选框也可以使用这个双向绑定我们先把这个值这个字符上改成一个布耳质默认给它处吧然后呢我们这里修改的时候呢把它等于它的相反值也就是处就变成falsefalse变成处然后在单选框中用VeganModel输入变量名保存一下我们就发现这个勾已经亮了当我们点击这个按钮的时候呢会触发这个函数会把它的值去反我们点一下不亮了再点亮了那这样呢单选框中就绑定上了这个值当我们打勾的时候呢这个值就变为处我们点掉的时候就变为false我们在代码中改变这个变量的值为触时它就亮为false的时候它就不亮了然后是动态绑定我们先把之前把那东西删一删哈我们可以看到这个盒子呢它的类名是item但其实我们还写了另外一个样式叫completed复制过来保存你会发现这个样式是这种带滑线的灰色的然后呢我们修改一下这个变量改为一个字符串就叫completed当我们点击这个函数值形的时候呢我们把它的值改为一个item也就是刚刚的样式这样的话呢我们就可以给这个class复制一个变量了就不是这种写死的了我们在它前面加上一个冒号然后呢这里就可以写上我们变量的名字保存我们发现它默认是completed也就是这个样式当我们点击的时候会改成item我们点击嗯 它的样式就改变了那除了动态绑定类名之外呢还有一个常见呢就是image标签image标签里面有个src也就是这个图片的url我们一般呢会用这个动态绑定的方式用一个变量的名填进来因为在网页中的图片不可能是写死的对吧我们实施从后端请求的时候改变变量的值这个图片就会改变那动态绑定类名呢一般也不是这种方式出现它会用这样的方式这个双印号中呢我们先写一个书组也就是这个中括号然后呢是一个布尔值我们这里直接写死好了写个处然后是一个三元运算符问号然后呢第一个类名completed第二个类名前面的这个布尔值来决定如果是 true的话呢就用第一个类名如果是false的话呢就用第二个类名动态把这个样式呢一般是这样写的我们刷新会发现它的这个类名是complete的如果我们改成false的话呢就是item那其实我们可以这样写把这个改成 true点击它的时候呢还是同样的曲反然后把这个变量放在这里那现在的意思是str的值呢是 true就是这个是false的话就是这个类名保存那么点击的时候呢这个str的值会这个曲反点击就会动态的改变它的样式了好我们把代码恢复一下然后来看列表渲染我这里创建了一个数组数组中有三个字符吃饭睡觉打豆豆那我现在需求是根据这个数组的个数比如它有三个动态的创建下面的这一条一条的代办事项也就是这个item怎么创建呢语法非常简单我们写v-forfor循环的这个for然后里面写的第一个是一个变量我们取名叫item这个名字是可以随机取的然后inin什么呢inlist就是我们这个数组的名字我们保存会发现已经动态的根据我们数组来创建三个了这个代码的意思是什么呢也就是根据这个数组中的个数来动态的创建这里面的东西有三个呢就创建三个有四个就创建四个那这个item呢就是数组中的每一项比如第一个item就是吃饭第二个是睡觉第三个又打兜兜那如何在标签中展示变量呢那view的语法是两个大括号我们这里写上两个大括号然后把这个item放到这个括号中来保存你就会发现吃饭睡觉打豆豆都已经展示出来了那这个v-for这里呢除了用一个item之外呢还能加另外一个参数叫index我们加个豆号隔一下按index这个index呢就是我们数组的下标我们也同样的展示在这个变量中0 1 2总结一下数组渲染呢是根据数组中的个数来创建多少个标签然后呢它有两个参数一个item就是数组中的每一项另外一个index就是下标这里还用了另外一个知识点在标签中展示变样呢用两个大括号就可以我们刚刚学了事件监听双向绑定动态绑定和列表渲染基于这些知识呢就已经可以完备这个to do list的了那我们就运用刚刚的知识把这个小案例写完再讲后面的知识点我们把代码复原一下代码复原之后呢我们分析啊需求首先我们得需要一个变量和这个输入框双向绑定获取我们输入的值我们创建一个变量这个变量呢默认为控然后呢我们还需要另外一个变量也就是一个数组来渲染这个代班式项的列表然后数组中呢应该是一个对象因为它有两项数一个是这个文本一个是一个不耳值来表示它是否被选中那数组我们也创建好了它里面每一项呢都一个对象然后有两个值标志的它是否完成而默认的是false然后它的文本我们写了吃饭睡觉打兜兜然后我们还需要两个函数一个就是点击的时候往这个数组中添加一下另外一个就是点击deal的时候我们删除一下我们首先写添加函数这里我们直接往list.value获取到这个数组然后往里push一条就可以了push呢我们同样的也会push一个对象这个值肯定是有的我们就默认为false然后这个text的值呢应该是什么应该是我们这个输入框中的字符所以我们把这个待会绑定到输入框的这个变量呢拿过来访问他的点value这个add函数就下好了然后还有一个删除函数我们写叫deal这个函数呢需要有一个参数了因为我们点哪一条这个代表实际上呢就删除哪一条不是乱删的所以我们这里需要一个index也就是这个数字的下标这里怎么删除呢我们直接list.value.split函数就可以了我们先出个index1有的出业者不知道这个split函数什么意思可以直接把它发给这个chartgpt问一下那它的意思呢就是从这个index这里开始删除删除几个呢删除一个那我们点这个deal的时候呢只要传一个数字的下标进来就可以了那数据准备好了呢我们就把它都绑定上首先是这个绑定输入框的变量需要用到双向绑定我们找到输入框写一个v-model然后我们这里填入这个变量的值就有双向绑定了当我们点击这个add to do按钮的时候呢需要执行这个添加函数所以我们在这里add click监听一下点击事件等于我们这个添加的函数再往下就是我们这个列表了需要根据我们的这个速度的长度来动态的渲染所以我们这里要写上v-for item in list那这里的值呢就需要换成我们item的text没错吧text就是这个文本保存看一下有没有问题好没有问题这里还有个单选框单选框需要绑定什么呢也同样的v-model需要绑定的就是我们item也就是每一个的isCompleted就是这个东西对吧我们默认绑定一下它是false没有被完成保存没有问题然后这里就是deo函数也就是我们如何删除异项了删除一项这里需要绑定一个add click我们的deal函数刚刚讲了我们删除每一项的时候都不是乱删的需要点哪个删除哪个所以需要传一个下标进来所以我们在v-4这里添加一个参数也就是index我们把index传入到我们的删除函数中保存我们测试一下添加没有问题点击这个样式还没有绑定我们找到我们的这个样式这里动态绑定样式把这个原始的样式删掉然后呢这里写一个数族第一个不耳直就是我们item.complete这个不耳直呢就表示着我们这一条有没有被完成问号如果它为tru的话呢我们就是completed这个样式如果不为tru的话呢我们样式就默认是item保存刷新一下再点一次OK可以被划线了我们测试一下添加一个比如学习viewok没有问题我们删除删除没有问题划线删除ok我们这个小案例就已经做完了这里还有个小问题我们添加完了之后呢输入框中值还是存在的案例说它应该被清空才对那我们只需要在我们的添加函数中再写上一句输入框中绑定的值给它清空等一个空字不上保存我们再来测试一下添加好这里清空了好 我们代码复原一下 继续学习这里看到我保留了一个字符串变量然后这个字符串变量双向绑定到输入框上了在我们以后写代码过程中可能会遇到一个需求就是监听某个变量的变化当这个变量变化的时候去执行某些操作也就是执行一些代码比如这里写一个function就随便取个名字吧就打印一个123我的需求是当这个变量变化的时候就执行这个函数这个变量什么时候变化呢我已经双向绑定到输入框上了当我在输入框中输入值的时候这个变量就变化了变化的时候呢请帮我执行这个函数在view中呢它叫真听器我们使用之前呢需要先引入名字叫watch它是一个方法所以调用的时候需要带一个小括号它需要两个参数第一个参数呢就是我们需要监听的变量这个str第二个参数呢就是我们的函数你要执行哪个函数我们这个add函数填进来好的这样就可以了当我们输入值的时候呢控制台上就会打出这个123也就是我们add函数就被执行了那有的时候呢我们还会需要这个变量变化前的值和变化后的值vill也帮我们提供了我们这里取票写两个参数就可以了我们起个名字叫new value和old value我们把它都打印出来为了更直观的看到认识呢我这里拼接了一点文字哈我们在这边输入1新的值为1旧的值为空我们这里是空字符上哈再输入2的时候新的值为12旧的值为1就可以拿到改变之前的值和改变之后的值了考虑这么一种情况我们监听的值不再是一个字符串或者数字了我们监听的值是一个对象这个对象有一个text的属性然后默认值也是一个空字符串我们这里绑定也绑定这个text然后保存我们在这里再输入的时候你会发现这个函数没有值行了为什么呢因为我们监听的是这整个对象整个对象并没有进行改变改变的只是对象里面的值如果想监听到这个值我们需要给Watch增加一个参数是一个对象然后里面有一个属性叫deep这个deep我们给个处保存再输入的时候就发现知情了这个东西我们叫深度减停在我们写代码的过程中有的时候会遇到动态展示的情况比如交由软件中的性别图标男生展示男生图标女生展示女生的图标但这两种图标在代码中肯定都是存在的只不过是动态的选择展示哪一个而已在vill中想实现这种效果是非常简单的比如这个标题我想让它不展示只需要加一行代码就可以了v-show那么这里写一个布尔值是 true或者false是 true的时候它没有任何变化是展示的为false的时候它就不展示了当然我们也可以把这个变量贴进来通过变量来控制它展示或者不展示在gs中能得到 true和false的东西有很多比如说各种比较运算符大于小于等于不等于与或运算符其实还有更简单的方式比如我们把这个值改成一个字符串如果这个字符串有值它就是展示如果这个字符串为空就不展示了我们把它改成数字如果数字有值也是会展示的如果数字为0也就不展示了除了微瘦之外微有还提供了另外一种语法叫微if微if和微瘦一样都接受一个不耳值如果是tree的话就展示如果是false就不展示那它们两个区别到底在哪里呢为了方便验证这个问题我们把这个veve写到最外面的这个盒子上也就是说如果为false的时候呢整个页面都不展示了如果为tru的时候呢整个页面都展示那我们点击检查元素首先选中这个最大的盒子这是我们网页中的标签那我当我vevevevefals的时候呢保存你发现这些标签全部都没有了那如果我们用Vshow呢来试一下你会发现虽然页面上不展示盒子了但是呢这些盒子还都是存在的我们的土毒app标题啊输入框都是存在的只不过呢他们都加了一个属性叫DisplayNum通过CSS的形式呢来让它隐藏所以由此可以得到Vshow呢盒子是会创建的只是把它隐藏起来而已那Veve呢连盒子根本都不会被创建了所以当我们需要频繁切换这个东西展示有不展示的时候呢我们就用vshow那如果从第一次渲染的时候就决定了是否创建我们就用vyeeve好 让我们最后一起来看一下关于组件的知识这个页面的代码呢已经被我删完了哈只留了一个盒子然后呢给他写的两样式假装呢它是一个按钮那首先什么是组件呢比如这个按钮在项目中我可能需要用到很多次每次用呢都重新写比较麻烦所以我就可以把它封装成一个组件用的时候只需要写个名字就可以了那组件可大可小只要是能共用的东西我们都可以封装成组件在view中每一个view页面都可以理解为是一个组件这里有一个components文件夹就是给我们放组件的我们右键新建一个文件我们叫button.view所有view页面都是以.view结尾的文件我们直接把这个页面的代码拷贝进来然后保存这样我们就创建好了一个组件回到我们的主页面把这些代码都删掉那么如何在这个页面中使用这个组件其实非常的简单我们只需要引入一下就好了我们通过关键字import引入一下然后是组件的名字组件的名字是我们字经意的甚至在不同的页面中我们都可以取不同样的名字我们这里叫mybutton然后是from关键字from关键字后面跟的是一个字符串这个字符串代表了我们文件的地址也就是我们刚刚创建的components底下的这个button.vill这里一定要注意了它提示补权的时候没有.vill结尾我们一定要把这个补上不然会报错的引入进来之后实用就非常的简单了就像我们写div标签一样写一个监控号然后写这个组件的名字我们取的名字叫mybutton直接复制过来然后一个监控号结束这样呢就是我们的组件了保存一下会发现这个按钮已经出来了那么想使用多个的时候呢就写入多个呗就可以使用多个了那外面这个组件呢我们称之为副组件里面这个组件呢我们称之为子组件那现在有个需求是我外面使用这个子组件的时候呢我希望里面的字是我传进去的而不是你写的固定的想时间这个功能呢需要用到vue中的prop也就是我们在直组件中需要定义一些变量然后副组件传进来首先就是定义定义比较简单我这import引入fromvue从vue中引入这么一个函数这个函数就是用来定义我们传进来的变量的也叫prop然后我们声明const props等于这是一个方法所以我们用小括号小括号里面我们输入一个中括号也就是一个数组数组中的每一项都是一个字符串这个字符串就是我们要声明的变量的名字比如我们声明了一个props叫text这个text就可以在副组件中传进来给我们使用那么在诸组件中如何使用这个props就直接在这里props.这个变量的名字text保存因为我们没有传入进来所以这里字都变成空的了那么需要在副组件中往里面传什么传入我们直接写这个text的名字等于号然后我们输入我们想要的组传比如说你好保存你会发现我们给每一个子组件传送不同的props就会获得不同的值我们从这里传进去了这里就开始使用了当然使用之前肯定是在子组件中已经定义过的没有定义过的值我们进行传送是没法使用的除了副组件往子组件里面传值子组件也可以跟副组件传值假设我这个按钮点击之后呢会进行一系列的数据处理处理完了之后呢我需要通知我的副组件我处理完成了并且带上我的参数那这个功能呢我先写写完了再告诉大家什么意思首先我们需要到直组件中来注册一个事件同样的我们需要引入一个东西然后呢我们来定义一个emit这里同样的事件的名字呢我们也数足字符上的方式我们这个事件就定义为ok吧我们写一个函数假设这里有一堂代码是用来处理数据的处理完了之后呢我们需要让副组件知道我们这里需要发射一个事件出来然后带上我们的参数这函数什么时候执行呢当我们点击这个按钮的时候执行然后我们在副组件这里监听一下我们刚刚这个事件叫什么事件OK事件等于我们自己的一个函数叫add有个参数接收一下保存点击Hello答应出来好的我们已经拿到了子组件传送出来的参数我们一起看看发生了什么首先我从veo中引入了一个工具类要来定义这个emit事件然后我定义了一个事件叫ok当点击这个按钮的时候就执行sign函数sign函数执行到这里的时候emit发出了ok这个事件也就是这个事件触发了并且我带了一个参数叫hello是个字符串我在副组件这里又监听了ok这个事件当这个事件触发的时候又执行了我的add函数又因为这个ok事件发生的时候带了一个参数在hello所以我在这里接收了一下这个参数并且把它打印出来这样我就实现了自往负的通讯并且带了我的参数最后总结一下我们今天学了vill的事件监听动态绑定双向绑定真听器列表渲染和条件渲染以及组件的注册和使用负质组件的相互通讯好了以上就是本期视频的所有内容了如果对你有帮助记得点赞投币关注我们下期视频再见拜拜
