---
title: "AI最强辅助！n8n+crawl4ai工作流，一键抓取任意网站！搭建RAG知识库+MCP自动化，让你的AI更准！更强！"
description: "视频讲解了利用n8n与crawl4ai构建自动化抓取工作流，并结合RAG技术搭建AI知识库，以提升大模型回答的准确性与时效性。"
pubDate: "2025-04-01"
favDate: "2025-04-13"
---
> **UP主**: 陶渊xiao明 · **时长**: 27:39 · [🔗 B站原视频](https://www.bilibili.com/video/BV1GqZbYWETd)
>
> **发布**: 2025-04-01 · **收录**: 2025-04-13
>
> **标签**: n8n · crawl4ai · RAG · 自动化 · AI工具

在AI的应用中有一项非常重要的技术就是RAG也就是检索增强生成它是一种结合信息检索和文本生成的AI方案工作流程是这样的用户向AI提问然后他会先去知识库中检索相关的内容然后呢把检索到的信息作为一个额外的上下文和用户的提问一起发送给大模型然后大模型结合用户的提问和检索到的信息来生成最终答案那由于大模型参考了外部的知识库所以呢它的回答准确性和时效性比普通的大模型会更好而在RAG中呢非常重要的一环就是知识库里面用到的数据那今天这期视频呢我们就来介绍如何使用N8N和Crawfall AI搭建一个数据抓取的工作流来自动化抓取网站的数据以及如何使用我们抓取到的数据搭建一个知识库来实现RAG方案这是搭建完成后的数据抓取工作流只需要输入一个网址它就能够自动的去抓取这个网站的全部的网页然后把它们转成Markdown文件保存到我们的电脑上然后呢我们再把这些Markdown文件加载到知识库中这样我们就可以使用RAG来进行问题查询了好了,下面我们就来一起搭建这样一个N8N工作流首先,我们打开N8N的Github项目那它的在线版呢,为个人用户提供了14天的免费试用我们这里还是选择本地部署的方式向下滑,在这个Quickstart这里呢给我们提供了两种本地部署的方式第一种呢是Node.js部署使用这个方式呢,我们需要先安装Node.js打开它的官网下载对应版本的软件然后安装就可以了安装完成以后我们复制这条命令打开这个命令行窗口我们首先执行这两条命令如果能够输出对应的版本号呢就证明Rodgis安装成功了接着呢把复制的命令粘贴上去然后执行执行成功了下面输出了N8N的本地网址按下O键它会自动打开网页这里就是我们使用这个Node.js部署的N8N服务那除了Node.js的部署方式呢我们还可以使用Docker来部署如果没有安装Docker可以在这里下载软件然后安装安装完成以后呢记得把它改成国内的Docker源我们这里复制它的第一条这个Docker命令然后执行那这条命令呢它创建了一个名字叫N8N Data的这样一个Docker数据卷它的作用呢是用来存储和持久化容器的数据这样呢即使这个Docker容器重启或者是被删除也不会丢失数据而且呢这个操作呢它是一个密等的操作重复执行也没有问题然后我们使用这条命令运行NBA容器可以看到这个命令中呢运到了一些参数我们在后面呢会详细的介绍这些参数的作用服务启动成功了我们打开这个服务的网址在这里就可以开始使用这个N8N了那接下来呢我们再来部署这个Craw for AI服务这个Craw for AI呢它是一个专门为大模型和AI应用设计的这样一个开源的爬充工具它能够自动抓取网页并且呢提取结构化的数据比如像Json,MacDone等等我们的工作理由呢需要使用它来抓取数据这里呢我们同样使用Docker的方式来部署这个Craw4AI打开我们的Docker软件然后在左边点击这个Docker Hub然后搜索Craw4AI我们选择第一项然后点击这个Tags可以看到在这里呢提供了很多个版本的镜像我们可以根据自己电脑的这个芯片来选择使用AMD或者是ARM的镜像比如我这里选择这个Organ AMD64回到命令行然后我们执行这条命令我们先来拉取这个镜像那大家可以根据自己的芯片呢把这个冒号后面的标签替换掉比如自己的芯片是ARM架构的就可以换成Organ ARM64或者呢直接使用这个Docker软件来拉取镜像也可以那镜像拉取完成了可以看到我们现在有两个Docker镜像是N8N的另一个呢是Crawl4AI下面我们来启动这个Crawl4AI容器我们执行这条命令那在这个命令中呢我们使用了这个-E参数设置了一个这个token的环境变量那这个token呢它是Crawl4AI它的API接口认证的时候需要的这个token的值呢我们这里设为了12345那这个值大家可以随意设置启动成功了这个网址就是这个Crawl4AI的网址我们点击打开它那记得呢要把这个0.0.0替换成127.0.0.1那如果打开网页以后显示的是Crawl4AI的操作文档就说明这个服务没有问题要使用Crawl4AI来抓取一个网站里面的全部的网页呢我们需要先获取这个网站的Sitemap.xml比如我们以Craw4AI它的操作文档的网站为例我们在它的网站的URL后面添加一个斜杠然后sitemap.xml可以看到在这个页面呢这里面有很多的URL那这些URL就是它的整个网站的所有的网页的URL所以呢我们只需要通过这个sitemap.xml就可以patch到它的整个网站了那比如DeepSec的API网站它也是一样的现在呢大部分的网站都有自己的这个Sitemap可以直接使用但是呢还是有一些网站它是没有配置Sitemap的那以我自己的这个博客网站为例我们打开Sitemap可以看到是没有对应内容的那这种网站应该怎么处理呢我们可以使用这个在线工具来生成一个Sitemap把网站的网址输入进去然后点击开始执行完成了然后我们点击这个查看按钮可以看到在这里呢就给我们生成了我这个博客网站的Sitemap我们可以直接使用它另外呢在抓取数据的时候我们还需要参考网站的robots.txt这个页面只需要在网址后面加上斜杠然后robots.txt就可以打开它那可以看到这个CrowforAI这个网站呢它是没有配置这个robots.txt的我们打开谷歌的看一下可以看到在这里面呢列出了哪些操作是允许的哪些操作是不允许的我们在抓取数据的时候呢最好遵守它的这些规范那现在我们这两个服务N8N和Crawfall AI都搭建完成了下面我们就可以开始搭建数据抓取的工作流了那对于N8N的使用如果不熟悉可以先看这期视频我们点击添加一个启动节点类型我们选择聊天消息点击打开聊天我们只要在这里呢发送一条消息就可以启动对应的工作流然后我们在启动节点后面给它添加一个http请求的节点那需要添加什么节点呢我们可以直接在这里搜索这个请求方法我们选择get然后url这里呢我们直接把左边这个chat input给它拖过来就可以了那这个chat input呢就是聊天的发送消息也就是说我们通过聊天消息把要抓取的那个网站的它的sitemap.xml对应的网址然后发送到聊天里面然后在这个http节点呢我们通过http请求去获取它的sitemap.xml中全部的那个网页的URL点击测试可以看到成功获取到了这个xml的内容那这里面呢就包含了我们要抓取的网站的全部的网页的URL但是呢这个内容它是XML格式的那为了翻面处理呢我们需要把它转成JSON格式我们点击添加一个XML节点然后选择这里把XML转成JSON点击测试一下可以看到XML的内容呢被转成了JSON格式那在这个JSON中呢有很多个URL这些呢都是我们需要抓取的那所以下一步呢我们添加一个Split节点那这个节点呢可以把这些URL切分成一个一个的那Fields这里呢直接把左侧的URL拖拽过来点击测试可以看到URL就被切分好了那接下来呢我们再添加一个Limit节点那这个节点它的作用呢相当于一个开关它允许我们呢可以只抓取几个URL而不是一次性抓取所有的URL那这样呢就方便我们调试那因为如果每次都抓取整个网站一是运行需要的时间比较长二是呢容易引发网站的反发策略这里呢我们限定只抓取前面两个URL在需要抓取整个网站的时候呢我们再把这个值给它调大就可以了点击测试可以看到它只保留了两个URL其他的都被过滤掉了接着呢我们再添加一个loop节点来循环处理每个URL这里我们选择每次处理一个URL点击测试没有问题然后把这个节点替换成一个http请求节点翻滑这里选择postURL这里我们粘贴上这个URL这个是我们前面使用Docker部署的Crow for AI它提供的抓取数据的API注意由于我们是使用Docker部署的所以这里的域名我们要使用这个host.docker.internal用它来替换掉127.0.0.1当然这里也可以使用反向代理来进行配置然后认证方式我们选择普通认证再选择请求头认证然后点击添加一个信任凭证那这里呢要认证主要是因为这个Crawl4AI它的API需要认证还记得我们在部署这个Crawl4AI服务的时候使用的那个docker命令它里面用到了一个-E参数配置了一个环境面量就是这个Crawl for AI API token等于12345我们需要认证的就是这个token那这个值呢我们是可以自定义设置的在信任凭证这里这个name我们输入authorization然后value这里我们输入bear空格然后12345这个12345就是我们前面设置的那个token点击保存然后我们打开下面这个发送消息体body那在这里呢我们需要添加两个消息体的字段我们打开这个Craw for AI的官方文档在这个使用示例中可以看到消息体需要两个字段URLS和这个priority这个URLS就是需要抓取的那个网页然后这个priority它是一个优先级可能和它的这个任务调度有关系我们把这两个参数来配置上UILS它的值我们直接把这个lock给它拖过来就可以了然后priority默认值我们给它设成10然后点击测试一下执行成功了那这里的返回结果呢是一个task id这里呢我们需要简单的介绍一下这个craw for ai它的工作流程那craw for ai呢它一共给我们提供了两个api一个呢就是我们当前配置的这个-craw结尾的这个那通过像他post的一个抓取请求呢他会给我们返回一个task id就是我们现在看到的这个结果然后使用他的第二个api我们再把这个task id给他传过去就可以获取抓取到的网页数据也就是说我们现在配置的这一步呢只是创建了一个抓取的任务而真正去执行这个抓取任务呢是在下一步进行的我们稍后呢会去配置它接下来我们添加一个wait节点那这个节点的作用呢是在每次抓取数据之前我们等待几秒钟这主要是为了防止我们抓取的频率太快从而会引发这个网站的反帕策略这里这个数值的单位是秒我们可以自己调整一个合适的数值点击测试没有问题那接着我们再来添加一个http节点那这个节点的作用呢就是执行抓取任务也就是我们前面提到的那个Crow for AI 的第二个API那方法我们选择geturl这里我们粘贴上这个url然后在后面我们把这个task id给它拖过来记得要把它前面这个空格给它去掉认证方式呢依然选择这个head认证还是使用我们前面创建的那个信任凭证点击测试可以看到执行成功了这里返回了抓取到的网页内容注意执行成功后返回的这个status它的字段的值是completed我们就可以使用这个字段来判断这次抓取数据的任务是不是执行成功了接下来呢我们再来添加一个EF节点来判断这个任务是不是执行成功了那在条件这里呢我们就使用这个statute的值是不是completed来进行判断测试一下没有问题那这个EF节点呢它是有两个输出的一个是true也就是任务执行成功另一个是false也就是任务执行失败我们在执行成功后面呢给它添加一个AI agent的节点这里我们选择自定义然后粘贴上我们的AI提示词它的作用是让AI帮我们把抓学到的数据整理成适合大模型驱动的RAG知识库的结构化一读格式的数据功能包括内容解析结构化整理创建FAQ提升可读性优化输出一共五项要求我们在左侧下滑找到这个Markdown字段把这个XXX删掉然后把markdown子段给它拖过来这样就可以了在右边可以预览一下这个完整的提示词点击关闭然后点击左下角这个加号我们给它添加一个AI模型这里我们选择Deepseek然后点击添加一个AI服务在这里需要输入Deepseek的API Key那这个可以去Deepseek的API开放平台来获取我这里之前添加过了那下面这里的模型呢我们选择Chat它对应的是DeepSeek的V3模型那这个模型它的价格呢更便宜而且最近有功能升级是能够满足我们需求的我们就选择这个模型点击测试可以看到在右侧呢输出了经过AI整理以后的内容那接下来我们需要处理一下这个数据抓取任务执行失败的情况我们点击这个EF节点的这个False分支然后给它添加一个自断编辑的节点然后在这里我们给它添加一个TaskID字段那它的作用呢是重新创建这个数据抓取的任务我们打开这个HTTP节点可以看到这个数据抓取它接受的任务参数呢就是一个TaskID所以呢使用这个字段面积节点我们就可以重新创建并执行失败的任务我们把这条线给它连到这个wait节点这样当这个任务失败以后它就能够被重新执行了好了现在我们的数据抓取没有问题了那接下来我们需要把抓取到的数据保存到我们的电脑上我们点击给它添加一个convert to file节点然后把左侧这个output拖到这个输入字段那文件名这里呢我们使用这个output的第一行点击测试可以看到这里生成了一个文件我们点击下载它那这个呢就是我们的Markdown文件可以看到整理的很不错在下面呢也按照我们的要求添加了这个FAQ的内容来更好的支持RAG而且呢Deepseek它还自动帮我们把这个英文的内容呢翻译成了中文但是呢现在这个文件它还是保存在N1N里面的并不会自动保存到我们的词盘我们还需要介入一个读写文件的节点来自动保存文件在操作这里我们选择写入文件到磁盘然后文件路径这里我们把这个output拖过来还记得我们在启动这个M1服务的时候呢使用了一个-v参数来进行路径映射那因为这个服务呢它是在容器中运行的所以呢生成的markdown文件默认会保存到容器的内部而不是保存到我们的磁盘上那使用这个参数呢可以把容器内的这个-home-node这个路径下的文件映射到我们本地磁盘的downloads-md这个路径下这样呢就能方便我们来使用所以呢这里的文件路径我们可以这样设置文件名这里我们使用输出内容的第一行当然也可以使用当前的时间好了现在我们这个数据抓取的工作流就搭建完成了我们来测试一下在这个聊天对话框中我们发送一个Sitemap的URL任务开始执行了在这里可以看到这个任务的执行进度执行完成了在左侧我们指定的位置生成了Markdown文件我们打开看一下可以看到结果文件也没有问题那如果用它来爬去整个网站能不能行呢我们来测试一下那首先我们把这个文件名改成使用当前时间来命名这样可以避免这个文件名过长然后在Limit节点这里呢我们让它爬取20个网页点击保存这次我们让它爬取Deepseek的API文档的网站点击发送它的Sitemap的URL开始执行了任务执行完成了可以看到在左侧20个网页对应的Markdown文件都成功生成了我们打开看一下这里面呢都是Deepseek的API文档数据都没有问题那接下来我们使用抓绝道的数据搭建一个知识库来试一试效果这里我们使用Terry Studio这个免费软件来搭建知识库关于Terry Studio的使用以及如何搭配本地AI如何配置MCP大家可以看这期视频我们点击知识库然后点击添加一个知识库名字叫做Deepseek-API迁入模型选择我们本地这个BGE-M3添加失败了那这里我们需要先打开Olama再来添加就可以了点击确定知识库创建完成了然后我们把这20个Markdown文件都给它加载到知识库中数据加载完成了我们可以在这里直接检索可以看到检索是没有问题的那接下来我们再来搭配本地AI使用这个REG方案来试一下效果我们打开AI对话页面然后点击左上角这里呢我们选择使用本地这个OLAMA部署的Deepseek 217B模型在对话框这里关联上我们刚才创建的这个Deepseek-API知识库然后我们问他如何使用Deepseek的API来获取可用模型的列表那可以看到他参考我们上传到知识库中的内容和大模型自己的知识给出了一个非常完整的回答从安装依赖到接口三数使用视力甚至接口的超时时间调用频率都包含在里面那可见使用RAG即使本地部署的AI呢也可以给出高质量的回答如果搭配在线大模型呢也可以提升回答的准确性和时效性这里有一个问题呢需要注意一下就是我们抓取到数据后使用在线的DeepSeek进行了数据的整理那如果要抓取的网页比较多呢我们需要关注一下这个消费的成本如果想使用本地的AI只需要把这个AI节点替换成我们本地OLAMA部署的AI模型就可以了但是由于网页的数据比较多所以使用本地AI的话最好要搭配显卡不然速度会很慢我们给这个工作流起一个名字然后点击这三个点再点击download这样就可以把这个工作流的配置文件下载到本地然后我们再新建一个工作流再点击这三个点然后选择这个从文件导入这样呢我们就直接导入了这个配置好的工作流那使用这种方式呢我们就可以把自己的工作流分享给别人或者呢直接导入别人分享的工作流来使用但是要注意在分享之前呢要删除掉配置文件里面这些敏感的信息比如像API Key等等而我们导入别人分享的工作流以后呢也需要把这些参数配置成自己的参数才能够使用最后我们再来介绍一下N8N的MCP功能我们在部署N8N服务的时候呢使用了这个-E参数配置了一个N8N的参数那这个参数的作用呢就是开启MCP功能那N8N的MCP功能呢它其实是借助了这个社区节点来实现的这里呢有它的使用方法要使用它我们需要先安装这个节点点击左下角的设置然后在左侧选择社区节点点击这个安装节点然后输入N8N-nodes-mcp点击安装安装完成了好了现在我们就可以在N8N中使用mcp功能了接下来呢我们用mcp来实现保存文件到本地这样一个功能我们用它替换掉这两个file节点点击添加一个AI agent节点它这个mcp需要挂载到这个AI agent节点上然后选择自定义这里输入这个AI提示词这个提示词的内容呢就是把生生的markdown写入MD文件然后保存到本地然后我们点击左下角添加一个AI模型它的作用是根据AI提示词自动调用合适的mcp工具这里也可以使用本地AI模型注意这里如果是使用原生的Deepseek我们一定要选择V3模型就是这个Deepseek-chat因为原生的Deepseek-re模型它不支持FactionCode是无法调用MCP工具的当然一些第三方部署的Deepseek-re它已经添加了对FactionCode的支持是可以使用的这里大家可以自行选择然后我们点击右边的Tools搜索mcp点击添加一个mcp节点然后在这里点击创建一个mcp上面这个类型我们选择标准输入输出然后在Command这里填入npx参数这里填写下面的内容那这个mcp server的作用呢就是操作稳定文件可以看到它的配置方式呢和cursor是非常相似的那关于mcp的使用和配置大家可以看这期视频下面的环境变量不需要配置我们点击保存在操作这里我们选择List Tools也就是列出这个MCP Server它支持的所有功能接着我们再添加一个MCP节点它的功能我们选择Execute Tool也就是执行工具然后在下面粘贴上这段内容它的作用是让AI来自行决定使用哪个工具然后下面的参数我们也交给AI来决定好了我们来测试一下我们把这个Limit改成1点击执行执行失败了这里提示没有找到Create File这个工具我们来修改一下提示词我们让它直接写入文件不要创建文件了因为它是有这个Write File这个工具的我们再执行一次又执行失败了还是提示没有找到这个Write File我们再来修改一下这里呢我们明确的告诉他使用这个write file这个工具点击执行这次终于执行成功了这里就是生成的文件其实这些文件呢都是生成在docker容器内部的只不过我们在部署服务的时候做了一个路径映射我们打开docker软件然后点击左侧的containers然后再点击我们这个n1容器然后点击这个files找到这个-home-node可以看到我们生成的文件都在这个路径下面然后这些文件呢被映射到了我们本地磁盘的downloads-md这个路径下点击这里可以打开查看这些文件那通过上面的演示呢我们可以发现这个N8N的MCP功能呢它不太稳定经过测试相同的这个AIT日词它有的收汇成功有的时候会失败大家可以通过修改提示词或者指定具体的工具来进行优化那在我们这个使用场景中我们还是切换回使用这个file节点来保存文件这种方式更稳定而且它减少了一次AI调用成本更低另外我们需要注意在抓取数据的时候要控制好频率避免被反帕策略限制好了关于如何使用N8N和Craw4 AI搭建一个自动抓取网站数据的工作流我们就介绍完了那视频中用到的资源和工作流配置文件都放在视频下方的介绍里面了感谢大家的观看和点赞支持我们下期视频再见谢谢
