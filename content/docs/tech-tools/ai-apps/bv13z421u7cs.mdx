---
title: "【官方双语】GPT是什么？直观解释Transformer | 深度学习第5章"
description: "视频通过可视化方式深入浅出地解析了GPT及Transformer架构的底层原理、数据流动过程及词向量转换机制。"
pubDate: "2024-04-30"
favDate: "2024-09-04"
---
> **UP主**: 3Blue1Brown · **时长**: 27:14 · [🔗 B站原视频](https://www.bilibili.com/video/BV13z421U7cs)
>
> **发布**: 2024-04-30 · **收录**: 2024-09-04
>
> **标签**: GPT · Transformer · 深度学习 · 大语言模型 · 神经网络

The initials GPT stand for Generative Pre-trained Transformer.So that first word is straightforward enough, these are bots that generate new text.Pre-trained refers to how the model went through a process of learning from a massive amount of data,and the prefix insinuates that there's more room to fine-tune it on specific tasks with additional training.But the last word, that's the real key piece.A transformer is a specific kind of neural network, a machine learning model,it's the core invention underlying the current boom in AI.What I want to do with this video and the following chaptersis go through a visually-driven explanationfor what actually happens inside a transformer.We're going to follow the data that flows through itand go step by step.There are many different kinds of modelsthat you can build using transformers.Some models take in audio and produce a transcript.This sentence comes from a model going the other way around,producing synthetic speech just from text.All those tools that took the worldbyStorm in 2022 like Dolly and Midjourney that take in a text description and produce an image are based on Transformers. And even if I can't quite get it to understand what a pie creature is supposed to be, I'm still blown away that this kind of thing is even remotely possible. And the original Transformer, introduced in 2017 by Google, was invented for the specific use case of translating text from one language into another. But the variant that you and I will focus on, olsa內容常常程的論源想ämän例如meChatGPT measurements prácticamente 是 Ron Jedman要讨kunём en能媒体 citinate maybe even with some surrounding images or sound accompanying it and produce a prediction for what comes next in the passage that prediction takes the form of a probability distribution over many different chunks of text that might follow at first glance you might think that predicting the next word feels like a very different goal from generating new text but once you have a prediction model a simple thing you could try生成長了一ثر文章的字接著按一個人搜集長的手伸出一張作詞讓 noi都是從nak只遍用於 Rust  Fraktion然後也鬆了解死的 fur又十分難但是如果торов übers webto selentGPT3就把 kop去架體 ge汽車一個很大的巨樣就是突然 還是 Aah 이건 Smokey因為這新的 истор组或者其他ор glassesell AIDS menu翻anos都是很多翻譯。我跟你拘erson,同一係當然用的 Sometimes,我會 delight著 communityallyש描述icial步, View ahökいる方法 ,我們 2022將開始 large manage the waylog如果一的一些 chatbots generates一句話, here's what's going on under the hood.首先, the input is broken up into a bunch of little pieces. These pieces are called tokens, and in the case of text these tend to be words, or little pieces of words, or other common character combinations. If images or sound are involved, then tokens could be little patches of that image, or little chunks of that sound. Each one of these tokens is then associated with a vector, meaning some list of numbers, which is meant to somehow encode the meaning of that piece. If you think of these vectors as giving coordinates in some very high dimensional space, words with similar meanings tend to land on vectors that are close to each other in that space. This sequence of vectors then passes through an operation that's known as an attention block, and this allows the vectors to talk to each other and pass information back and forth to update their values. For example, the meaning of the word model in the phrase a machine learning model is different from its meaning in the phrase a fashion model.The attention block is what's responsible for figuring out which words in the context are relevant to updating the meanings of which other words,and how exactly those meanings should be updated.And again, whenever I use the word meaning, this is somehow entirely encoded in the entries of those vectors.After that, these vectors pass through a different kind of operation,and depending on the source that you're reading this will be referred to as a multilayer perceptron, or maybe a feedforward layer.然後這裡每一張不要話說一話,別 の真相同的反看,現在這quest semua動作,...稍後會兒我多想 recognize how this step是一個多好問問お題然後 emptying them based on answers to this. Florence 他們所有動作 Changxi枱模樣 advisory process is to können read the underlying matrices.則我從頭幾乎 slider取詐固的 prepared共識 ,結果 결과上升過這些 trước來就是不終究 Doug Future就要慢慢ches後來ουν後原本的意思都是完全 chorus昨 AbbVeter架果回家密接近on特指这类的私自的只是细一种 gut text叽量来接着就eed tells you一扯重鸟 decir这里可以估计自己的需要质感公 snapshot视频每天上的季节而rab结断 是捏与下的写真彆细 kann看上owskithrough...Better知道的 Check GPT的关系最早的新的展示esten是gpt3的新典说比如手记在写真正中到即as因为核心相 Tinderto make a tool like this into a chatbotthe easiest starting point is to have a little bit of text that establishes the setting of a user interacting with a helpful AI assistantwhat you would call the system promptand then you would use the user's initial question or prompt as the first bit of dialogueand then you have it start predicting what such a helpful AI assistant would say in responsethere is more to say about an added step of training that's required to make this work wellbut at a high level this is the general ideaMING PAOUNTIn this chapter, you and I are going to expand on the details of what happens at the very beginning of the network,at the very end of the network,and I also want to spend a lot of time reviewing some important bits of background knowledge,things that would have been second nature to any machine learning engineerby the te度在景色一些一起啊,我相信這則是最新的今天影片  Guinness Generation овые幾多類活了之前進入了設計劇 也是挺好的至少大家現在認為  Автом Method節 ième提示主要 Eigen除了 each other  neoliberate把言論 которые毀動時走天彈tery Music Жành where you're using data to somehow determine how a model behaves. What I mean by that is, let's say you want a function that takes in an image and it produces a label describing it, or our example of predicting the next word given a passage of text, or any other task that seems to require some element of intuition and pattern recognition. We almost take this for granted these days, but the idea with machine learning is that rather than trying to explicitly define a procedure for how to do that task in code, which is what people would have done in the earliest days of AI, Instead, you set up a very flexible structure with tunable parameters, like a bunch of knobs and dials, and then, somehow, you use many examples of what the output should look like for a given input to tweak and tune the values of those parameters to mimic this behavior. For example, maybe the simplest form of machine learning is linear regression, where your inputs and your outputs are each single numbers, something like the square footage of a house and its price,你看到了一 encontramos其他 according to標準 друга市場,不管 intrinsic價值逆費 寄陷入的距離計算 organization làome個ys method, qui功 trifle bi Oke,可 longer OpenÉ實際上,深誇的考量更容易 GPT-3,加 choVAL有122分,但运 name 可You can create some giant model with a huge number of parametersWithout it either grossly overfitting the training dataOr being completely intractable to trainDeep learning describes a class of modelsThat in the last couple decadesHave proven to scale remarkably wellWhat unifies them is that they all use the same training algorithmIt's called backpropagationWe talked about it in previous chaptersAnd the context that I want you to have as we go inIs that in order for the training algorithm to work well at scale这些开封招系继续一个些轻 mee的变 및 195 00后,这能源自然是继续的商量考虑精进行。这些问题的解决也是用方式的有单种的流技法,会否或是这些甚大heus際 clearer解释。ett Sp Orleans,或者是每个什么 kitty gases,这些式像是一种理解释为 tan丝重量 coron Samt最能他是一个科路 一个科dy处理者,或者可以eros执 dabде有数个 konfigurע acaba BundesIA通过 knew Varx的具体渠は重量开始来扬装你会发 registRL Tั Pe《新的大家好》的almost always referred to as weights, and this is because a key feature of these models is that the only way these parameters interact with the data being processed is through weighted sums. You also sprinkle some nonlinear functions throughout, but they won't depend on parameters. Typically though, instead of seeing the weighted sums all naked and written out explicitly like this, you'll instead find them packaged together as various components in a matrix vector product. Macht现在��这也ska咱说 FINLcin在icle syieren Ä'.每把神类的器材 Alo está究竖cer。 roof相对是最为了按经浏鸡的 compt sav MakroOSűs AmaTROCAS mà放任何ey hep形容赶是 ama在 burdened的榛 isolー Antony Ancak那是 Pon advertisers所ostat? Bernabeu California, 175B üst parti我built DuBGT3 아주住hor Ian ARM塞 into just under 28000 distinct可呢這些數字 fall into 8 different categories, and what you and I are going to do is step through each one of those categories to understand what that type does. As we go through, I think it's kind of fun to reference the specific numbers from GPT-3 to count up exactly where those 175 billion come from. Even if nowadays there are bigger and better models, this one has a certain charm as the first large language model to really capture the world's attention outside of ML communities. teşekkür inqual therein whether human application未来注意公司的眼神就站在 stories上方多次看 сегодня晚 că前往我們看看一氏 ấu egy君臆, almost all computers look like Matrix Ta趨 Alex光方案 небольш兆 permit太 косmet Yeah!我印象三個不重要, medicine for the weights of the model which I ll always color in blue or red and the data being processed, which I'll always color in gray. The weights are the actual brains. They are the things learned during training, and they determine how it behaves. The data being processed simply encodes whatever specific input is fed into the model for a given run, like an example snippet of text. With all of that as foundation, let's dig into the first step of this text processing example, which is to break up the input into little chunks and turn those chunks into vectors.我們點à charlie它們已經話一 axe,這是總 que一些詣禱每個未必要μη徒一項目的統計 known as the embedding matrix, has a single column for each one of these words. These columns are what determines what vector each word turns into in that first step. We label it wE, and like all the matrices we see, its values begin random, but they're going to be learned based on data. Turning words into vectors was common practice in machine learning long before transformers, but it's a little weird if you've never seen it before,是整個給了ức的一些東西才知道一直飛往就會開始懂情習慶我們 often Fellows Dit叫的我們常叫做這個這個, which invites you to think of these vectors very geometrically, as points in some high-dimensional space. Visualizing a list of three numbers as coordinates for points in 3D space would be no problem, but word embeddings tend to be much, much higher-dimensional. In GPT-3, they have 12,288 dimensions, and as you'll see, it matters to work in a space that has a lot of distinct directions.同樣是你就可以採取一個2D面子一圈的空間來然後從那3D面子到那個圖來自動的說明就是想要做的我會做個長度的動作選擇一圈圈圈圈圈圈and projecting the word vectors down on to that and displaying the results.The big idea here, is that as a model tweaks and tunes its weightsto determine how exactly words get embedded as vectors during training,it tends to settle on a set of embeddingswhere directions in the space have a kind of semantic meaning.For the simple word to vector model I'm running here,if I run a search for all the words whose embeddings are closest to that of tower,you'll notice how they all seem to give very similar tower-ish vibes.如果你想去就在 neut质 perto State, apostles得就表 Airbus创傷了節奏,雖然是raum dating,但你很直 cs Murray stated,另一種概念故utan descents is 만약,你思考材過是這樣,其量度 др肥一 respect虛仩和男 Kristoff严 nós habla的是有點像這át牙 hogy向一 embaixo的 guided辟近雖然比特 총分不大II You Didn't Know The Word For A Female MonarchYou Could Find It By Taking KingAdding This Woman Minus Man DirectionAnd Searching For The Embeddings Closest To That PointAt Least Kind OfDespite This Being A Classic ExampleFor The Model I'm Playing WithThe True Embedding Of QueenIs Actually A Little Farther OffThan This Would SuggestPresumably Because The Way That Queen Is Used In Training DataIs Not Merely A Feminine Version Of KingWhen I Played AroundFamily Relations Seem To Illustrate The Idea Much Better THE POINT IS, IT LOOKS LIKE DURING TRAINING THE MODEL FOUND IT ADVANTAGEOUS TO CHOOSE EMBEDDINGS SUCH THAT ONE DIRECTION IN THIS SPACE ENCODES GENDER INFORMATION. ANOTHER EXAMPLE IS THAT IF YOU TAKE THE EMBEDDING OF ITALY, YOU SUBTRACT THE EMBEDDING OF GERMANY, AND THEN ADD THAT TO THE EMBEDDING OF HITLER, YOU GET SOMETHING VERY CLOSE TO THE EMBEDDING OF MUSSOLINI.它是and monster one bit of mathematical intuition that's helpful to have in mind especially forthe next chapter is how the dot product of two vectors can be thought of as a way to measurehow well they align computationally dot products involve multiplying all the correspondingcomponents and then adding the results which is good since so much of our computation has to looklike weighted sums geometrically the dot product is positive when vectors point in similar directions是0,1 dans�를ho bows 해� mình Step 1 假如这支表例子,你连讨所以让状况提示S這個 BlaHarata的 executive O Jack-P Tree niveau symbol主持人解私底部的形式 greeting我去模棄ar和這組成-98C acknowled情简式與XTR用符合的詞語。如果大家玩一玩,你會看到這些詞語的詞語真的肯定會非常高的價值指引他們更高的詞語,指他們更合適合這條詞語。 It's also fun,如果我們用符合這個詞語詞語的詞語1,2,3, and so on, they give increasing values, so it's as if we can quantitatively measure how plural the model finds a given word. Again, the specifics for how words get embedded is learned using data. This embedding matrix, whose columns tell us what happens to each word, is the first pile of weights in our model. And using the GPT-3 numbers, the vocabulary size specifically is 50,257,而且,不可能是用的,不只是用的,而用的。而,这个组织的组织是12288,变成了617,000,000,000,000,000,000.我们再加上这个组织,记得,在最后,我们应该是175,000,000,000,000,000.在这个情况下,我们要认为这些组织的织织织,而一 Kings诺拟投入更多 位置导致文并 auth difícil wonder而定性 性 sort of读到文 owners everyone will talk about later and more importantly你会认为要精神科菜地描く给他们充满意味道 A Noah二は事後,自分 horrific的途 bracket在文的过程中, 并毫无法解放适的 histor Itj Casual тебя to that by the end it points in a way,̶子 更 最感的角度 所揭会的条件, Google constructed或建立将来对之前的fall 科学过以前的国家,但是他是被已决ulated , 他者这是能够在表 Videon di消息 你 ум词这个词, Ent模 würden善根的译 要有通知的细节自由这是有利的意思 是当时音ika就是非常长 Old distance away 教引可能的把 SOL& 明研究经理讯的是wią going  üxtend包括细节的语言 那个照顾让绳能 Baba Sum,  bağ Olá 维俗达成细节的方法要很明 William,その前 쉿的余 autant是合董里的质員一種就是拋ído到捨KM,所以提供ela的只有一種倉脂 without no口 ling称。以資生理念为摄鋼管它通过是os拌有的眼 síness形的放在65 subscribeOOOO 5The network can only process a fixed number of vectors at a time, known as its context size. For GPT-3, it was trained with a context size of 2048, so the data flowing through the network always looks like this array of 2048 columns, each of which has 12,000 dimensions. This context size limits how much text the transformer can incorporate when it's making a prediction of the next word. This is why long conversations with certain chatbots, like the early versions of ChatGPT,普通銀利� thisiew我ZT?...都是有關於考慮的方片放延時會把最透過的事情都提升ט算入時間但是以後剛開始我應該要問得現多少 lipsticktogether could full embrace. we'll definitely start at the next future! remember, the desired output is a probability distribution over all of the tokens that might come next. for example, the last word is professor, and context includes words like harry potter and immediate preceding, we see least favorite teacher了解我使用检物 sexualmäß二种线ikels似乎innerы,然后 emitindo星- deles不可能提供航诺者?这个遵纪业��起热是另一種tr例 vais用一个 MATRIX枪极限上尤其辣植核正 catching最后50,000ciaグラ这个iy persecution Arrow性总是一种途径 Sunday代表这个 grund築 מס我们将当ir言える之前小伙大娅也算了,就是 Rafi。前浴的能值要做最主义的是 o最終博 당시的 wn下缺 sir這反enge的是 1000多個 left Voor pulled Dann著我們自己,她自己的位置也有多少體操作為什麽此為一個非常能力 які利用各川內在抽離數bro 거북三是出這些關י後來得到크� сл赤再 colleagues我要先再yim lips一而且这一ficiente关键的公式是名物 Minh这个内容就是给我们的 Chung我们的是ление写的ível rank一部分的 recruitment但正面还是在知义的练习股仗'我们ท佤黄件净我们的挑战状詞已经有一个 $0我们的 quietly nom迎语拿 지역 among the笠hou和您是同类我们的配件 is很伤 нас而是非常相� drink的但是它留在一个 чет half所以它加了 section 617 estiver viewing características的 meaning our count so far is a little over a billion a small but not wholly insignificant fraction of the 175 billion that we'll end up with in total as the very last mini lesson for this chapter I want to talk more about the softmax function since it makes another appearance for us once we dive into the attention blocks the idea is that if you want a sequence of numbers to act as a probability distribution say a distribution over all possible next words then each value has to be between 0 and 1而你也需要每一次加上一的一但是如果你是在玩的 deep learning game在每一種都做法都看起來就像是個數字的你得到的結果不太能夠在這裏等於是一種的,或是一種多,而他們並不一定加上一種Softmax是一種常用的方法把一種種的分析 into a valid distribution in such a way that the largest values end up closest to 1, and the smaller values end up very close to 0. That's all you really need to know. But if you're curious, the way that it works is to first raise e to the power of each of the numbers, which means you now have a list of positive values, and then you can take the sum of all those positive values and divide each term by that sum, which normalizes it into a list that adds up to 1.如果一枚的一枚在它的 input是大量的比例,在它的 output,它是在它的导致的导致所以你只是在导致的导致你只能在导致的导致但是它是比较导致的导致在其他导致的导致在其他导致的导致它也有导致导致的导致而且它們的导致整個导致的导致在一些情況下像在 ChatGPT用這個导致to create a next word,there's room for a little bit of extra funby adding a little extra spice into this functionwith a constant t thrown into the denominator of those exponents.We call it the temperature,since it vaguely resembles the role of temperature in certain thermodynamics equations.And the effect is that when t is larger,you give more weight to the lower values,meaning the distribution is a little bit more uniform.And if t is smaller,then the bigger values will dominate more aggressively. where at thed are, set t sagt 0 means all soda很寸的ieu区例如,總共 SungInalı runsGPTP3使用GPTP3並用的文字 用EPT3成功地上 caused the seed  Halloween一集,在 inn稼僵中, � tall Пр是A零分寸Ey take away betweenczonymFx undMapT3名而保able 0 멤�oringyi用Fx都加強 LegMaster ses una stosific DoC后来大概是一个产手打天的 coalition 以后才出现了或者小领来的对于亚力大48 cm  mism MIT!妈咱们站在一马 两个疫情, in equival Con Chi这 This website他就可以ところ了一小� tym波数不在 персонаж上但是这 bridge的会认真的向你所担心的好操作传검而言 portugu gesehen已经� chang派一个ybAD批划主要一个president manera的战略我就希望他们的东西人 were seenıld anything Nag seus取 trustees若是你 ל� Memḥ在这 mercado的目光Nex bein dob 23 07% everyone最可能的 NEXT tokens, which seems to be the maximum they'll give me, and then I tweak the probabilities based on an exponent of 1 fifth. As another bit of jargon, in the same way that you might call the components of the output of this function probabilities, people often refer to the inputs as logits, or some people say logits, some people say logits, I'm going to say logits. So for instance, when you feed in some text you have all these word embeddings flow through the network, and you do this final multiplication with the unembedding matrix, machine learning people would refer to the components in that raw, unnormalized output as the logits for the next word prediction. A lot of the goal with this chapter was to lay the foundations for understanding the attention mechanism, Karate Kid wax on wax off style. You see, if you have a strong intuition for word embeddings, for softmax, for how dot products measure similarity, And they need some kind of計劃, that all the calculations have to look like matrix multiplication, with matrices full of tunable parameters, then understanding the Attention Mechanism, this cornerstone piece and the whole modern boom in AI, should be relatively smooth. For that, come join me in the next chapter. As I'm publishing this, a draft of the next chapter is available for review by patreon supporters. A final version should be up in public in a week or two it usually depends on how much I end up changing On the meantime If you want to dive into attention And if you want to help the channel out a little bit It's there waiting
