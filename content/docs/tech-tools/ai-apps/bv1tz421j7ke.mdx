---
title: "【官方双语】直观解释注意力机制，Transformer的核心 | 【深度学习第6章】"
description: "视频深入浅出地讲解了Transformer模型中注意力机制的原理，包括词嵌入如何通过向量方向表达语义以及多头注意力的运作方式。"
pubDate: "2024-05-04"
favDate: "2024-09-04"
---
> **UP主**: 3Blue1Brown · **时长**: 26:10 · [🔗 B站原视频](https://www.bilibili.com/video/BV1TZ421j7Ke)
>
> **发布**: 2024-05-04 · **收录**: 2024-09-04
>
> **标签**: Transformer · 注意力机制 · 深度学习 · 大语言模型 · 3Blue1Brown

刚才是清單紫的 wave上所,rillullie前進行行展示 imagen genre division有一等 Scandinavian技術中在大語言模式中iendo很多用法,實際上, StreamCase karena 他们的ên投稫有联系当初面 つ nod 있습니다 этим picked by揭上了心境下的此刻,in dan alan它們的手 politics揭穫wareContext的中心就跟此重點始ál啊,此性最重要的地方有關 immer您的adh 해봐The goal of the model that you and I are studying is to take in a piece of text and predict what word comes next.The input text is broken up into little pieces that we call tokens,and these are very often words or pieces of words,but just to make the examples in this video easier for you and me to think about,let's simplify by pretending that tokens are always just words.The first step in a transformer is to associate each token with a high dimensional vector,what we call its embedding.The most important idea I want you to have to mind is how DIRECTIONS in this high dimensional space of all possible embeddings can correspond with semantic meaning.In the last chapter we saw an example for how direction can correspond to GENDERin the sense that adding a certain step in this space can take you from the embedding of a masculine noun to the embedding of the corresponding feminine noun.That's just one example.You could imagine how many other directions in this high dimensional space landmark主要是多个多music Sad key Antarctica半iggerまあ别忘了對方就不可能 fresh,我们已在资料等 técnica доб binnen所以說在上面也要力 scientist,我們要考慮一些 這類的錯來的有種眼數 setzt始終的 beraber一樣我們提出 dont眼望看一種aries染料 belang都 directly照在這3個因為這個一кт token embedding是修往一個 Clack是 Subt印竣的這些 feathers並無説指於這特關的廠odel只有在該頓步的這 Ex Cuba candidate、其破證拔回應到先用的這個而言 Π the picture you might have in mind is that there are multiple distinct directions in this embedding space encoding the multiple distinct meanings of the word mole and that a well trained attention block calculates what you need to add to the generic embedding to move it to one of these more specific directions作为重点的解释再次重点,看来的词,用了一种形式的词是,这些是很不具体的,不具体的形式与很多大,大,多的词如果这词是直接遗孙遗,你会想要的 mechanism去运动这个词因此,它们在一个方式的方式更具体的词, may be correlated with vectors associated with Paris and France and things made of steel. If it was also preceded by the word miniature, then the vector should be updated even further so that it no longer correlates with large, tall things. More generally than just refining the meaning of a word, the Attention block allows the model to move information encoded in one embedding to that of another, potentially ones that are quite far away, and potentially with information that's much richer than just a single word.我們也就是說三 gaan我們лам after all the vectors flowing通的玉 ile各種人寫程度連結Works都是計畫實的多分主要是來 Bellevuls的僅殻圖一個終一個在選擇質。中一個現象指 Elijah其實當然會有一半一些已經Ovel最大全層的跟 fica因為姻思商卻是如果ança apostle《UL shown》達到です第0題目計的結子那最後一個這一個混帳但又变说了很多www可以让到没 MIC≽更多的指数数字 depending функ不及以外, acc offered更多的 이�級코ja从真正方的解放这个数字 approved the next word再来一下比课 télé就是在翻转拦到 angel增的概念合适合的 read comments现在我对 mention的是我们是不是 että the attention jagota一定要演出了 Oberon的闪力是很多多续闪而ed卻套设坛重ics的码原 dividend全能辨计程 commodities对这就是 unquote卡只能在这트� Apps而且那不是差不多但是另外 instead לה концлаг套设法底的非常 comun的有很多為人ãos有的 puedo说更多此前但現在,Are YOU需要的出現現是這次賽的 함유부只要 business和相場的解法而让它們所共同的 listener現在就辨 IBM的Viece這個目標是電子很多動畫做一個整個整體的把設計 locally belang individual прок entsprech有자를因應邀和內容的發動遊玩這隊 Wa zaten Northern解ок要很多人各地 rec passado要質nya vect recognized where the matrices are full of tunable weights, things that the model will learn based on data. To be clear, I'm making up this example of adjectives updating nouns just to illustrate the type of behavior that you could imagine an intention had to doing. As with so much deep learning, the true behavior is much harder to parse because it's based on tweaking and tuning a huge number of parameters to minimize some cost function. It's just that as we step through all of the different matrices filled with parameters that are involved in this process,我认为是很容易有一个例子的例子让它们可以做到更加确实的方法一开始的一步,你会想每个词,像是词词请问,如果有词词,有词词在前面然后,词词词和白色,可以每个词词答应Yeah,I am an adjective,and I am in that position!That question is somehow encoded as yet another vector,another list of numbers,which we call the query for thimplo,是其他國方的問博物给我一个個字,其人缴短衰的 tens 물�Ấn無法,这个决箱格,ありün guest,帮我们让CMS的结束。这个合格上看这个anutwить LOC,这是文化文字的简IE和 WHAT心',我们能看到这个不要混 le圈在作房地裡。每次你把我放了一圈,像这一圈,就是要把这个圈的圈在这个圈的圈中给你这个圈的圈在这个圈中你这种圈的圈在这个圈中一圈圈的圈这些圈圈是一种圈的圈这种圈圈的圈所以,它是真正的圈在这种圈圈的圈圈在这种圈圈的圈圈我们 за对我们想要关注測增 这个渠stal的 Gleich樹表示我们的想法, 例如这是 америк错诽考虑指定的共 ?" caps PNG那种 которая鬆了一些矩� chaque主要的内容κ Current Ch Nicolas diaild是 Story Name的想法绝对其他避免标不уют知道可能又是新细 cert法斯楷了其他增 erstmal好现在是同时间的笔題您刚才中 Yea,同时 with that还有另一个黄千参cz是用的千 waiteduna which you also multiply by every one of the embeddings produces a second sequence of vectors called the keys Conceptually, you want to think of the keys as potentially answering the queries this key matrix is also full of tunable parameters and just like the query matrix, it maps the embedding vectors to that same smaller dimensional space you think of the keys as matching the queries whenever they closely align with each other in our example, you would imagine that the key matrix maps the adjectives像 Fluffy and Blue to vectors that are closely aligned with the query produced by the word creature. To measure how well each key matches each query, you compute a dot product between each possible key-query pair. I like to visualize a grid full of a bunch of dots where the bigger dots correspond to the larger dot products, the places where the keys and queries align. For our adjective noun example, that would look a little more like this, where if the keys produced by Fluffy and Blue真的比理性和ああ 還好me我們 politico當你對共有的結迫氣Y导聽我們來共有的對連個百分它就是隨著隨著七辣เล我们的组织是一个组织的组织,我们可以把整个组织的组织,让我们一个组织的组织,让我们把组织的组织是去更多的组织,我们将来使用这些组织是把组织的组织,在每个组织的组织,在每个组织的织织,所以我们要把组织的织织是为了解织织织,我们要是为了解织织织织 between 0 and 1,每个织织织织织织织织,像是一치提我们叫这个群细节的关系如果你看到原本的 Transformer paper有一个很简单的方法是在这些描述的这里, variables q and k represent the full arrays的 query and key vectors, respectively这些小细节你得到 by multiplying the embeddings by the query and the key matrices这些描述在这个 numerator是一个很简单的方法 represent the grid of all possible dot products between pairs of keys and queriesA small technical detail that I didn't mention is that for numerical stability, it happens to be helpful to divide all of these values by the square root of the dimension in that key query space. Then this softmax that's wrapped around the full expression is meant to be understood to apply column by column. As to that v term, we'll talk about it in just a second. Before that, there's one other technical detail that so far I've skipped. During the training process, when you run this model on a given text example, and all all the weights are slightly adjusted and tuned to either reward or punish it based on how high a probability it assigns to the true next word in the passage, it turns out to make the whole training process a lot more efficient if you simultaneously have it predict every possible next token following each initial sub-sequence of tokens in this passage. For example, with the phrase we've been focusing on, it might also be predicting what words follow creature and what words follow the.這就是很不錯,因為它可以讓一種例子的例子可以作為例子的例子可以作為例子我們要注意的例子我們要不斷讓例子的例子讓例子的例子因為他們可以讓例子的例子讓例子的例子我們要這些例子這些例子這些例子的例子可以讓例子的例子可以作為0最簡單的例子但是如果做了,就不再加一一,就不再是一,所以不再是一般的方法所以,一般的方法是,之前用 Softmax,你把所有的 entries都放在-infinity如果做了,就在做了一種, allemaal各種 trips, Мы donne parree所以大家也有unkt意外呢你想看出,多оме比增足看到最近 years have seen some variations to the attention mechanism aimed at making context more scalable but right here you and I are staying focused on the basics. OK, great. Computing this pattern lets the model deduce which words are relevant to which other words. Now you need to actually update the embeddings, allowing words to pass information to whichever other words they're relevant to. For example, you want the embedding of fluffy to somehow cause a change to creature that moves it to a different part of this 12,000-dimensional embedding space that more specifically encodes a Fluffy creature. What I'm going to do here is first show you the most straightforward way that you could do this, though there's a slight way that this gets modified in the context of multi-headed attention. This most straightforward way would be to use a third matrix, what we call the value matrix, which you multiply by the embedding of that first word, for example, Fluffy. The result of this is what you would call a value vector, and this is something that you add to the embedding of the second word in this case something you add to the embedding of creature so this value vector lives in the same very high dimensional space as the embeddings when you multiply this value matrix by the embedding of a word you might think of it as saying if this word is relevant to adjusting the meaning of something else what exactly should be added to the embedding of that something else in order to reflect this looking back in our diagram我们把所有的确件和确件的确件因为你继续做了你把这个 value matrix然后把每一种的确件的确件成为一个整个确件的确件你可能认为这些确件的确件是相当的确件的确件每个确件的确件在这个确件你把每个确件的确件的确件是在确件的确件的确件例如,在这里,在确件的确件you would be adding large proportions of the value vectors for fluffy and bluewhile all of the other value vectors get zeroed out or at least nearly zeroed out and then finally the way to actually update the embedding associated with this column previously encoding some context-free meaning of creatureyou add together all of these rescaled values in the columnproducing a change that you want to add that i'll label delta eand then you add that to the original embedding希望, what results is a more refined vector encoding the more contextually rich meaning, like that of a fluffy blue creature. And, of course, you don't just do this to one embedding, you apply the same weighted sum across all the columns in this picture, producing a sequence of changes. Adding all of those changes to the corresponding embeddings produces a full sequence of more refined embeddings popping out of the attention block. Zooming out, this whole process is what you would describe一只 head of attention. As I've described things so far, this process is parametrized by three distinct matrices, all filled with tunable parameters, the key, the query, and the value. I want to take a moment to continue what we started in the last chapter with the scorekeeping where we count up the total number of model parameters using the numbers from GPT-3. These key and query matrices each have 12,288 columns matching the embedding dimension128 rows, matching the dimension of that smaller key query space. This gives us an additional 1.5 million or so parameters for each one. If you look at that value matrix by contrast, the way I've described things so far would suggest that it's a square matrix that has 12,288 columns and 12,288 rows, since both its inputs and its outputs live in this very large embedding space.如果真的,这会认为150000000有提��的� Does that have!你一定可!但是比 both原來 огром,Med근 אבל monkey的大比,iferation版,这边是要更加诶�这是pind begin because i in this elder mind if this three lihat fromorganizer Anyway, I still encourage you think about the overall linear map One with inputs and outputs both in a larger embedding space For example, taking the embedding of blue to this blueness direction that you would add to nouns It's just that it's broken up into two separate steps The first matrix, on the right here, has a smaller number of rows看到它的大小小一个 hammerám大块这个 jurcan的是不 су的物上伪otta点表示我访问是在 resource�诿上传达的我认为话見这种lik温 give膘了点点to throw in linear algebra jargon here what we're basically doing is constraining the overall value map to be a low rank transformationturning back to the parameter count all four of these matrices have the same size and adding them all up we get about 6.3 million parameters for one attention headas a quick side note to be a little more accurate everything described so far is what people would call a self attention headto distinguish it from a variation that comes up in other models that's called cross attention This isn't relevant to our GPT example but if you're curious, cross-attention involves models that process two distinct types of data, like text in one language and text in another language that's part of an ongoing generation of a translation, or maybe audio input of speech and an ongoing transcription. A cross-attention head looks almost identical. The only difference is that Key and Query maps act on different datasets.在一模式的程式中,例如,the keys might come from one language,while the queries come from another,and the attention pattern could describewhich words from one languagecorrespond to which words in another.and in this setting there would typically be no masking,since there's not really any notion of later tokensaffecting earlier ones.staying focused on self-attention though,if you understood everything so far,and if you were to stop here,you would come away with the essence of what attention really is.但全部üs sk necessaryいris sudah要寫到覆过意语法在其中で由于我们的 Heyo我們在点凭認識变成但是 Klima很多不同的 illustrations你们评论 syndromk ,我們普通見 adjectives更好 cred?但是當然,我們主要有大家 nanbre,自己有更多《ufesting》 性話話場所表論簡介了風格。 Scroll...第ongời詞和聲解 tweeted motor。其實這是 assez有一點的說法。一些容易造成的 發聲比要rolle CC-K,如果使用《辨isode》算還是 Harry,這個ryIL成了 Powersatos wil。炎強的翻譯、雷封和 Scripture稱, then perhaps the embedding of Harry should instead be updated to refer to the prince. For every different type of contextual updating that you might imagine, the parameters of these key and query matrices would be different to capture the different attention patterns, and the parameters of our value map would be different based on what should be added to the embeddings. And again, in practice, the true behavior of these maps is much more difficult to interpret, where the weights are set to do whatever the model needs them to doto best accomplish its goal of predicting the next token. As I said before, everything we've described is a single head of attention,and a full attention block inside a transformerconsists of what's called multi-headed attention,where you run a lot of these operations in parallel,each with its own distinct key, query, and value maps. GPT-3, for example, uses 96 attention heads inside each block. Considering that each one is already a bit confusing,it's certainly a lot to hold in your head.直接. to spell it all very explicitly, this means you have 96 distinct key-and-query matrices producing 96 distinct attention patterns. Then each head has its own distinct value matrices used to produce 96 sequences of value vectors. These are all added together, using the corresponding attention patterns as weights What this means is that for each position in the context, each token, every one of these heads produces a proposed changeto be added to the embedding in that position. So what you do is you sum together all of those proposed changes,one for each head,and you add the result to the original embedding of that position. This entire sum here would be one slice of what's outputtedfrom this multi-headed attention block,a single one of those refined embeddingsthat pops out the other end of it. Again, this is a lot to think about,so don't worry at all if it takes some time to sink in.the overall idea is that by running many distinct heads in parallelyou're giving the model the capacity to learn many distinct ways that context changes meaningpulling up our running tally for parameter count with 96 heads each including its own variation of these four matrices each block of multi-headed attention ends up with around 600 million parameters there's one added slightly annoying thing that i should really mention for any ofyou who go on to read more about transformers, you remember how I said the value map is factoredout into these two distinct matrices, which I labeled as the value down and the value up matrices. The way that I framed things would suggest that you see this pair of matrices inside each attention head, and you could absolutely implement it this way. That would be a valid design, but the way that you see this written in papers and the way that it's implemented in practice looks a little different. All of these value up matrices for each head估 cour Even Rama зачем,那种 Gaia, kitty… Küsch id informação KIM 是 number two end定 paint Un jemanded attention계 Some people refer to the value matrix6 They're typically ONLY referring prata till but the value down To the one I'm labeling I've putt an onscreen note These details are one that's pretty sturdy point But I'll call it out so that you can read also尤其是, NEGLE點예요, と samo往 vimos較多的是auдICSabSys, 關鍵供貨物的 enlightenmentCIALSOCUS asınt只能導入同時 Gone процесс ge孵到 下一項 include 的都有成長種是多少件事的其中的, There are many more chances for this more nuanced embedding to be influenced by its more nuanced surroundings. The further down the network you go, with each embedding taking in more and more meaning from all the other embeddings, which themselves are getting more and more nuanced, the hope is that there's the capacity to encode higher level and more abstract ideas about a given input beyond just descriptors and grammatical structure, things like sentiment and彩音, whether it's a poem, and what underlying scientific truths are relevant to the piece, and things like that. Turning back one more time to our scorekeeping, GPT-3 includes 96 distinct layers, so the total number of key, query, and value parameters is multiplied by another 96, which brings the total sum to just under 58 billion distinct parameters devoted to all of the attention heads.这 hiç充足 是全个多的,但是cop派510 Chris充分派720要称这个组织的 Network员所以那些比较なので保证鐊 concent资ation包括什么ton Pasad阿边却在比较导致这些以及现在将你们和我一九降检的路人也请多产领埪三冬 but also包括� Team还有这个铃花等一番之后的故事 这是 ayr多不ちょ幷得出现的特别性牙反控不是很忘记出来的但至于是无学的 meaning that you can run a huge number of computations in a short time using GPUs. Given that one of the big lessons about deep learning in the last decade or two has been that scale alone seems to give huge qualitative improvements in model performance, there's a huge advantage to parallelizable architectures that let you do this. If you want to learn more about this stuff, I've left lots of links in the description. In particular, anything produced by Andrej Karpathy or Chris Ola tend to be pure gold.in this video i wanted to just jump into attention in its current form but if you're curious about more of the history for how we got here and how you might reinvent this idea for yourself my friend vivek just put up a couple videos giving a lot more of that motivation also brit cruz from the channel the art of the problem has a really nice video about the history of large language models优优独播剧场——YoYo Television Series Exclusive
